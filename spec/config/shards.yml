production:
  shards:
    alone_shard:
      adapter: mysql
      host: localhost
      database: octopus_shard5
    
    history_shards:
      aug2009:
        adapter: mysql
        host: localhost
        database: octopus_shard2
      aug2010:
        adapter: mysql
        host: localhost
        database: octopus_shard3
      aug2011:
        adapter: mysql
        host: localhost
        database: octopus_shard4
    
    country_shards:
      canada:
        adapter: mysql
        host: localhost
        database: octopus_shard2
      brazil:
        adapter: mysql
        host: localhost
        database: octopus_shard3
      russia:
        adapter: mysql
        host: localhost
        database: octopus_shard4